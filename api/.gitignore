node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Fotos enviadas de produtos — ficam num volume Docker, não em versionamento
/src/uploads/produtos/*
!/src/uploads/produtos/.gitkeep
